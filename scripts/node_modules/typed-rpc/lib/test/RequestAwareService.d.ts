import { Service } from "./service";
/**
 * A `Service` implementation with access to the request headers.
 */
export declare class RequestAwareService implements Service {
    private headers?;
    constructor(headers?: Record<string, string | string[] | undefined> | undefined);
    hello(name: string): string;
    greet(hello: string, name?: string): string;
    sorry(name: string): string;
    echoHeader(name: string): string | string[] | undefined;
}
